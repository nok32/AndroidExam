<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kiril.examproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACTION_DIAL"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".FoodStoreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/category_pizza"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="db_foods_store.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.example.kiril.examproject" />

        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".FoodsActivity" />

        <activity android:name=".BaseActivity" />

        <activity android:name=".ShoppingCarActivity" />

        <service android:name=".services.EatService"/>

        <receiver android:name=".receivers.NotificationReceiverBreakfast">
            <intent-filter>
                <action android:name="com.example.kiril.examproject.services.BREAKFAST" />
            </intent-filter>
        </receiver>
        <receiver android:name=".receivers.NotificationReceiverLunch">
            <intent-filter>
                <action android:name="com.example.kiril.examproject.services.LUNCH" />
            </intent-filter>
        </receiver>
        <receiver android:name=".receivers.NotificationReceiverSupper">
            <intent-filter>
                <action android:name="com.example.kiril.examproject.services.SUPPER" />
            </intent-filter>
        </receiver>

    </application>

</manifest>